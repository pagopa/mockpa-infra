{
  "id": "paywallet",
  "displayName": "Payment Wallet ${env}",
  "indexTemplate": {
    "indexPattern" : "logs-${env_separator}paywallet-*"
  },
  "dataStream": [
    "logs-${env_separator}paywallet-default"
  ],
  "dataView": {
    "indexIdentifier": "${env_separator}paywallet"
  },
  "customComponent": "default"
}
