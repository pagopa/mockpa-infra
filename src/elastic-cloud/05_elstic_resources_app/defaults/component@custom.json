{
  "template": {
    "settings": {
      "index": {
        "lifecycle": {
          "name": "${lifecycle}"
        },
        "default_pipeline": "${pipeline}"
      }
    }
  },
  "_meta": {
    "description": "Settings for ${name}"
  }
}
